version: '3.8'

services:
  toyamas-ollama:
    image: ollama/ollama:latest
    container_name: toyamas-ollama
    restart: always
    ports:
      - "${OLLAMA_PORT}:11434"
    volumes:
      - ./ollama_data:/root/.ollama
