version: '3.8'

services:
  toyamas-redis:
    image: redis:7-alpine
    container_name: toyamas-redis
    restart: always
    command: redis-server --requirepass ${REDIS_PASSWORD}
    ports:
      - "${REDIS_PORT}:6379"
    volumes:
      - ./data:/data
