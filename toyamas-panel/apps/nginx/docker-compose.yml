version: '3.8'

services:
  toyamas-nginx:
    image: nginx:alpine
    container_name: toyamas-nginx
    restart: always
    ports:
      - "${HTTP_PORT}:80"
    volumes:
      - ./html:/usr/share/nginx/html
